[package]
name = "clash-subscription-info"
version = "0.3.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
execute = "0.1.0"
regex = "1"
chrono = "0.4.0"
